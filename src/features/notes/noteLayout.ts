export const minimumNoteWidth = 350;
export const minimumNoteHeight = 350;

export const noteLayoutCssVars = {
  '--note-min-width': `${minimumNoteWidth}px`,
  '--note-min-height': `${minimumNoteHeight}px`,
} as const;
